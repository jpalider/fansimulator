#include "Monitor.h"

Randomize Monitor::randomize = Randomize();
std::vector<Server> Monitor::servers = std::vector<Server>();
Time Monitor::clock = Time();
EventList Monitor::agenda = EventList();

int main(){
	
	
	

	return 0;
}
